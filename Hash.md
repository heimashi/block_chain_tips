# Hash tip

### Hash 散列 哈希
* 把任意长度的输入，通过散列算法，变换成固定长度的输出，该输出就是散列值。
* 三个特征
	* 输入任意大小
	* 输出省固定的  例如md5是128位 sha256是256位
	* 对应n位的字符串其哈希值计算的复杂度是O(n)

### 加密的哈希函数
三个附加的特性：
* 碰撞阻力
	* 碰撞是指两个不同的输入产生了相同的输出
	* 虽然输入空间远远大于输出空间，理论上是必然会出现碰撞，但是例如Sha256哈希算法，输出空间2^256要测出碰撞目前计算能力还没能找到碰撞。而md5已经被证明会出现碰撞
	* 应用：信息摘要

* 隐秘性 或者不可逆
	* 根据输出值没有可行的办法算出输入值，不可逆
	* 应用：承诺

* 谜题友好
	* 想找到y值所对应的输入，假定在输入集合中，有一部分是非常随机的，那么他将非常难以求得y值对应的输入
	* 应用：搜索谜题 挖矿
	
### 哈希指针及数据结果
* 区块链

* Merkle trees
	* 应用 隶属证明 非隶属证明